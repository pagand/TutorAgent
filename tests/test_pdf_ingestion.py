# tests/test_pdf_ingestion.py
# Keep empty or add tests that verify side effects visible via API
# e.g., check if hint quality improves after ingestion (hard to automate)
# or check collection count via ChromaDB client (more unit-test like)
# Relies mostly on the setup fixture for now.